/* Component Chalk Variables */
@use 'sass:math';
@use 'sass:map';

@use './mixins/config';

/* <%= name %> */
$<%= name %>: () !default;
$<%= name %>: map.merge(
  (
  ),
  $<%= name %>
);
